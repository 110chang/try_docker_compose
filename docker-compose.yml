app:
  build: "./app"
  container_name: "app"
  working_dir: "/usr/src/app"
  ports:
    - "8080:8080"
  volumes:
    - "$PWD/app/src:/usr/src/app"
  links:
    - "redis"
redis:
  image: "redis:3"
  container_name: "redis"
